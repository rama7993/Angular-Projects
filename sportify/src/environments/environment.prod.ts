export const environment = {
  production: true,
  // Kept for backwards compatibility with `BackendService` (no longer required for Deezer).
  backendApiUrl: 'https://sportify-backend-beta.vercel.app/api/spotify',
};
